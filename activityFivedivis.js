console.log("/m");
for (let i = 0; i < 6; i++) {
    let randomNum = Math.floor(Math.random)() * 30;
    if (randomNum % 7 === 0) {
        console.log(`Random number ${x} is divisible by 7`);
    } else {
        console.log(`Random number ${x} is NOT divisible by 7`);
    }
}