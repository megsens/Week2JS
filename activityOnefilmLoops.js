let films = ["Avengers: Endgame", "Wandavision", "Pride and Prejudice (2005)", "Deadpool", "Harry Potter"]; // initial 5 films
films.push("Iron Man", "Captain America: Civil War"); // ".push" method to add two films
console.log(films); // logging the arrays to the terminal to make sure the code runs

for(let i = 0; i < films.length; i++) {  // Loops through the array items

    console.log(films[i]);                  // Displays results to the terminal

}